TODO list

* Fix memory leak with single-ended proxies
* Querying by association
* Loading proxies
* Redis specific dynamic methods (random entry, flushing, savedb, bgsave etc.)
* Spring PlatformTransactionManager support
* Plugging in validation
* Connection pooling for RedisClient