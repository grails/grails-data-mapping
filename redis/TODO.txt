TODO list

* Fix memory leak with single-ended proxies
* Querying with sort by property name
* Support for simple projections (property, distinct, max, min, sum)
* Support range queries (gt, lt, gte, lte, between) using sorted sets for indices
* Redis specific dynamic methods (random entry, flushing, savedb, bgsave etc.)
* Spring PlatformTransactionManager support
* Plugging in validation
