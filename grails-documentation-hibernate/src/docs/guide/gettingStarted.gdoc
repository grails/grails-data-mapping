To use GORM 5.x and above in Grails 3.x to use GORM for Hibernate 4 you can specify the following configuration in @build.gradle@:

{code}
dependencies {
    compile "org.grails.plugins:hibernate4:VERSION"
    compile "org.hibernate:hibernate-ehcache"
}
{code}

Where @VERSION@ is 5.0.0 or above.